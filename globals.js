const init = () => {
    let substrates = [];
    let plans = [];
    let crops = [];

    global.substrates = substrates;
    global.plans = plans;
    global.crops = crops;
};

module.exports = {init};
